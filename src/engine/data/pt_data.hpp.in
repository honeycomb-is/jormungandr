#pragma once

#include <cstddef>

namespace Engine::Data
{

    // Embedded JSON payload for the periodic table (generated at configure-time)
    static constexpr const char PT_JSON[] = R"PTJSON(@PT_JSON_CONTENT@)PTJSON";
    static constexpr std::size_t PT_JSON_LEN = sizeof(PT_JSON) - 1;

} // namespace Engine::Data
